echo "Enter the value of n:"
read n
sum=0
count=1
while [ $count -le $n ]; do
 square=$((count * count))
 sum=$((sum + square))
 count=$((count + 1))
done
echo "The sum of squares of the first $n numbers is: $sum"
